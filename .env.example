# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/flipflop_print

# Optional: PostgreSQL connection details (if using separate variables)
PGHOST=localhost
PGPORT=5432
PGDATABASE=flipflop_print
PGUSER=username
PGPASSWORD=password

# Node Environment
NODE_ENV=production